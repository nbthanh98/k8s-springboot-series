server:
  port : 8081

spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: ${MYSQL_URL:jdbc:mysql://localhost:32621/payment}
    username: ${MYSQL_USERNAME:payment}
    password: ${MYSQL_PASSWORD:VNTechies2023}
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true